# 前端环境变量示例

# 统一走 Nginx 反代：浏览器请求 /api -> Nginx -> backend:8080
VITE_API_BASE_URL=/api

# 生产环境如果前后端不是同源（不走 Nginx /api 反代），可设置后端源站
# 例如：https://api.example.com
# VITE_BACKEND_ORIGIN=

# 静态资源/用户上传资源的 CDN（可选）
# 例如：https://cdn.example.com 或 COS accelerate 域名
# VITE_CDN_URL=

# TipTapEditor 直传 COS 用（可选；不配置则需要代码里有默认值）
# VITE_COS_BUCKET=
# VITE_COS_REGION=ap-hongkong
